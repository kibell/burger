<li>
	{{burger_name}}

	<button class="change-devour" data-id="{{id}}" data-newEat="{{devoured}}">
		{{#if devoured}} yum!{{else}}zzzz{{/if}}
	</button>


	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>